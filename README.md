# spam-email-classifier
The spam email classifier project is a machine learning application designed to automatically categorize emails as spam (unwanted or harmful messages) or ham (legitimate emails). The project involves collecting and preprocessing email data by cleaning text, removing irrelevant words
